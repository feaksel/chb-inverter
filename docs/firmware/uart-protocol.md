# UART protocol

!!! info "Phase 2 stub"
    Full content arrives in Phase 2 — extracted from [Build Guide v4.0 — §8.7](../hardware/build-guide-v4.md) and the firmware CHANGELOG.

The operator dashboard talks to the STM32 over a single UART link, 115200 8N1. The frame format and command set are defined in §8.7 of the build guide; this page surfaces the same content in a navigable per-command reference once Phase 2 has imported the firmware.
